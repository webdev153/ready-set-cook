{
  "name": "ready-set-cook-mobile",
  "version": "1.0.0",
  "description": "Ready Set Cook! — Android wrapper (Capacitor)",
  "private": true,
  "scripts": {
    "sync:www": "node ../scripts/sync-www.js",
    "sync": "npm run sync:www && npx cap sync",
    "build": "npm run sync:www && npx cap sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
